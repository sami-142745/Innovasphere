# Local Development Configuration - Innovasphere v2.0
# Activates with: -Dspring.profiles.active=local

spring:
  datasource:
    url: jdbc:mysql://nozomi.proxy.rlwy.net:37468/railway?createDatabaseIfNotExist=true&useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: gEgNkPnVLtKmmYKcHfcDfzZHAVnEbBXs
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: InnovasphereHikariPool

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true

  jackson:
    serialization:
      write-dates-as-timestamps: false

  main:
    lazy-initialization: true

server:
  port: 10000
  address: 0.0.0.0

app:
  jwt:
    secret: 9Mb0dxoggo3iVxWsTKAqx2D0Cfe5+QxwtfEGeotN+Xq6S67WrEsdGaTwYdZnqeVWgyzjfGIRONKpL6+DcqPrag==
    expiration-ms: 86400000

  cors:
    allowed-origins: http://localhost:5173,https://innovasphere-frontend.onrender.com

  seed:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: INFO